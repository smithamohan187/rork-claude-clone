import React from 'react';
import UserProfileScreen from '@/app/user-profile';

export default function ProfileTab() {
  console.log('[ProfileTab] Rendering TouchPoint profile as tab root');
  return <UserProfileScreen />;
}
